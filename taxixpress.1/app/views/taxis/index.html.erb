<p id="notice"><%= notice %></p>

<h1>Listing Taxis</h1>

<table>
  <thead>
    <tr>
      <th>Type taxi</th>
      <th>Numbre seats</th>
      <th>Date create</th>
      <th>Date update</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @taxis.each do |taxi| %>
      <tr>
        <td><%= taxi.type_taxi %></td>
        <td><%= taxi.numbre_seats %></td>
        <td><%= taxi.date_create %></td>
        <td><%= taxi.date_update %></td>
        <td><%= link_to 'Show', taxi %></td>
        <td><%= link_to 'Edit', edit_taxi_path(taxi) %></td>
        <td><%= link_to 'Destroy', taxi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Taxi', new_taxi_path %>
